# 💊 HellBot — Токсичный Напоминатель Таблеток

**HellBot** — это Telegram-бот с характером, который заставит тебя следить за здоровьем. Он использует метод "кнута и
пряника" (в основном кнута), мемы, черный юмор и пассивную агрессию, чтобы ты не забыл выпить свои лекарства.

> *"Не сдох — уже молодец. Горжусь, бл\*ть."* — HellBot

---

## 🔥 Возможности

* **Мульти-таймеры**: Устанавливай сколько угодно напоминаний (утро, обед, вечер).
* **Персистентность (Persistent)**: Таймеры сохраняются в файл. Если бот перезагрузится или упадет, он **восстановит**
  все задачи при запуске.
* **Токсичная мотивация**: 100+ фраз: от заботливых до угроз удалить историю браузера и аккаунт в Steam.
* **Система Ачивок**: Статистика приемов, расчет веса съеденной "химии", уровня кибернетизации и забавные звания (от "
  Подопытной крысы" до "Бога биохакинга").
* **Мемы**: Отправляет случайный мем при подтверждении приема.
* **Чистый чат**: Бот автоматически удаляет старые уведомления и свои сообщения через 10-30 секунд, чтобы не засорять
  переписку.
* **Удобный интерфейс**: Инлайн-клавиатуры для выбора времени и управления.

---

## 🛠 Установка и Запуск

### 1. Требования

* Python 3.10+
* Токен бота от [@BotFather](https://t.me/BotFather)

### 2. Установка (Linux / Arch / macOS)

**Клонируйте репозиторий (или создайте папку):**

```bash
mkdir hellbot
cd hellbot
```

**Создайте виртуальное окружение:**

```bash
python -m venv .venv
source .venv/bin/activate
```

**Установите зависимости:**

```bash
pip install -r requirements.txt
```

### 3. Настройка

Создайте файл .env по примеру .env.example и заполните его

```
BOT__TOKEN = "ВАШ_ТОКЕН_ЗДЕСЬ"
```

Вставьте туда токен, который вы получили от BotFather.

### 4. Запуск

```bash
docker-compose up --build -d
```

---

## 📂 Структура проекта

* `bot.py` — Основной код бота. Все фразы, логика и клавиатуры находятся здесь.
* `config.py` — Файл с конфигом
* `texts.py` — Файл с текстами
* `timers.json` — Автоматически создается ботом. Хранит активные таймеры пользователей (чтобы не терялись при
  перезапуске).
* `stats.json` — Автоматически создается ботом. Хранит статистику (кто сколько выпил).
* `.venv/` — Папка с виртуальным окружением и библиотеками.
* `requirements.txt` — файл с зависимостями.

---

## ⚙️ Настройка фраз (Кастомизация)

Хочешь сделать бота добрее или еще злее?
В файле `texts.py` есть списки, которые можно редактировать:

* `MEMES` — Ссылки на картинки (JPG/PNG).
* `PHRASES` — Список текстовых угроз и напоминаний.
* `STARTUP_PHRASES` — Фразы, которые бот пишет в консоль при запуске.
* `STATS_PHRASES` — Реакции на просмотр статистики.

---

## 🚀 Деплой (Хостинг)

Чтобы бот работал 24/7, рекомендуется запустить его на **VPS** (Ubuntu/Debian) или сервисах вроде **PythonAnywhere**.

**Пример запуска на сервере через Systemd (чтобы сам оживал):**

1. Создать файл `/etc/systemd/system/hellbot.service`
2. Прописать конфиг:
   ```ini
   [Unit]
   Description=HellBot Service
   After=network.target

   [Service]
   User=root
   WorkingDirectory=/root/hellbot
   ExecStart=/root/hellbot/.venv/bin/python bot.py
   Restart=always

   [Install]
   WantedBy=multi-user.target
   ```
3. `systemctl enable hellbot && systemctl start hellbot`

---

## 🛡 Лицензия

Этот код написан для личного использования и спасения печени. Распространяется по лицензии "Делай что хочешь, только пей
таблетки".

---
